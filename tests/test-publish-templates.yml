name: Test publish templates inputs
on:
  workflow_dispatch:
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2
      - id: action
        uses: .
        with:
          publish-templates: 'true'
          base-path-to-templates: 'src/templates'
          oci-registry-for-templates: 'ghcr.io'
          templates-namespace: 'test-org/templates'
          disable-schema-validation: 'false'
          disable-repo-tagging: 'true'
          generate-docs: 'false'
        continue-on-error: true
      - name: Assert action ran with templates inputs
        env:
          OUTCOME: ${{ steps.action.outcome }}
        run: |
          echo "outcome: $OUTCOME"
          # The action will fail because there are no actual templates to publish
          # and no OCI credentials, but it must have run (not skipped)
          if [ "$OUTCOME" = "skipped" ]; then
            echo "FAIL: action was skipped, expected it to run"
            exit 1
          fi
          echo "PASS: action ran with oci-registry-for-templates and templates-namespace inputs, outcome=$OUTCOME"
